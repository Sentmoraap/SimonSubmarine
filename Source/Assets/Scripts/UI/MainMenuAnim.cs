using UnityEngine;
using System.Collections;

public class MainMenuAnim : MonoBehaviour {
    public Animator cr;
	// Use this for initialization
	void Start () {
        cr.SetBool("Walk", true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
